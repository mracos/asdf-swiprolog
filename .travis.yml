language: c
script:
  - asdf plugin-test swi-prolog $TRAVIS_BUILD_DIR 'swipl --version'
before_script:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install --force readline libarchive; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew link --force readline libarchive; fi
  - git clone https://github.com/asdf-vm/asdf.git
  - . asdf/asdf.sh
os:
  - linux
  - osx

addons:
  apt:
    packages:
      - libxinerama-dev
      - libarchive-dev
